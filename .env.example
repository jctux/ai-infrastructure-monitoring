# Vault Connection Configuration
# All application secrets are stored in HashiCorp Vault
VAULT_ADDR=http://10.1.0.99:8200
VAULT_TOKEN=your-vault-token-here
VAULT_SECRETS_PATH=ai-infrastructure-monitoring

# Optional: Override default secrets path
# VAULT_SECRETS_PATH=custom/path/to/secrets
